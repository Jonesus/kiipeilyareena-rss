[project]
name = "ka-rss"
version = "0.1.0"
description = "Scrape Kiipeilyareena news and generate an RSS feed"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "beautifulsoup4>=4.13.0",
    "feedgenerator>=2.1.0",
    "requests>=2.32.0",
]

[tool.uv]
dev-dependencies = []
